{
  "name": "inspection-lite",        // Project name
  "type": "module",                 // Enables ES module syntax (import/export)
  "version": "1.0.0",               // Version of your app
  "main": "server.js",              // Entry point when app runs
  "scripts": {
    "start": "node server.js"       // Run your app using `npm start`
  },
  "dependencies": {                 // External libraries the app uses
    "express": "^4.18.2",           // Web server framework
    "lowdb": "^6.0.1",              // Lightweight JSON database
    "cors": "^2.8.5"                // Middleware to handle cross-origin requests
  }
}
